# Socket Multiplayer Framework — 任务落地书

> 状态：**P0 ✅ · P1 旧方案已废止 · P2 M1 ✅ · P2 M2 核心 ✅ · M3-S 同步基线 ✅ · 对局内核/五子棋规则 ✅ · Mirror 五子棋桥接 ✅ · M3 生命周期待落地 · P4 待落地**（2026-09-02 更新）· 双实例运行验收待执行
> 🔍 **2026-09-11 四代理核查**：P2.16/P2.17 已完成（文档过时）；P2.15 语义重定义；P2.19 重连 0 实现；M4.6 前需修断线死锁等缺口；P4 两个硬阻塞。详见各节注记与文末"关键文件速查"修正。
> 配套文档：`01_主流Mirror框架调研报告.md`（为什么做）、`02_落地实施计划.md`（怎么做）
> 使用方式：每完成一项勾选 ✅；每阶段完成打一个 git 提交。当前路线不包含强制物理同步和回放，分别由 P6/P5 的触发条件决定。

### 进度记录（P0 已完成；P1 旧方案已废止）

- **P0 全部完成**：生成器 `Regenerate Demo`、`NetworkPlayer` 去 config 字段、`ServerReturnToLobby` 校验 + 统一 command 路径、HUD 去重、README。
- **P1 历史方案已废止**：曾按 `NetworkRoomManager`、`SocketRoomPlayer` 和双 prefab 方案完成过一版，但因全局场景切换不适合单端口多房间，已由 M2 重映射为 `NetworkManager + RoomRegistry + NetworkMatch`；相关记录保留在本任务书第三节。
- **P1 编译验证**：Unity batchmode 多次编译 0 错误；headless 重新生成 `MultiplayerGenerated` 成功，Bootstrap 场景已指向 `SocketRoomManager`，无 `SocketNetworkManager`/`playerPrefabOverride` 残留。
- **P1 期间修复的坑**（详见 git 差异）：① `roomSlots` 基类 cast；② 基类 `OnServerDisconnect` 先移除 roomSlots 导致 Leader 转移失效 → 改从 `conn.owned` 解析；③ `TryStartGame` 恢复"全员就绪"门；④ 入口场景 Lobby→Bootstrap（manager 仅在 Bootstrap）；⑤ `allowLateJoiners` 默认改 false（基类不支持中局加入，P2 处理）；⑥ 生成器残留 `FindProperty("config")` 空引用。
- **复审修复（2026-08-31）**：🔴 HUD 按钮空操作 → `RoomOperations` 从 Lobby 场景移入 Bootstrap 的 DontDestroyOnLoad manager 对象（HUD 在 Awake 时 Lobby 未加载、且只在 Awake 查一次 → 按钮全废；且若仅按"OnGUI 重查"修，切进 Game 场景后 Lobby 卸载、`room` 重查为 null，Lobby 返回键同样会死，故必须同对象共存）+ HUD OnGUI 每帧兜底重查。🟡 `allowLateJoiners` 纯死字段 → 加 Tooltip 声明保留待 P2；P2 必须 override `OnServerConnect/OnServerAddPlayer` 才能真正消费（P1 的红线"不改 8 个入口方法"仅限 P1）。🟡 `autoStartWhenAllReady` 基类 flip 死锁 → 改从 `ReadyStatusChanged`（每次就绪变化都回调）自行统计，`OnRoomServerPlayersReady` 置空抑制基类默认 `ServerChangeScene`。
- **M2 架构重调（2026-08-31）**：P1 的 `NetworkRoomManager` 全局切场景方案已被 `04_M2重新映射方案.md` 覆盖。当前实现改为 `SocketRoomManager : NetworkManager` + `RoomRegistry` + `NetworkMatch/MatchInterestManagement`，单一 online scene 内支持多房间；`SocketRoomPlayer` 仅保留为旧资产兼容壳，新生成链路不再使用双 prefab。
- **M3-S 同步基线（2026-08-31）**：玩家 Transform 已统一为服务器到客户端；输入改为带序号的 Command，服务端增加乱序/重复/过快帧过滤；新增协议版本与配置签名握手、`SocketAuthenticator`、`SocketNetworkMetrics`；Bootstrap 与 Player 生成资产已同步更新。
- **对局内核与 Mirror 桥接（2026-09-02）**：新增网络无关的 `MatchSession`、`MatchParticipant`、确认事件流、状态快照和离线回放游标；新增 `GomokuRules`、`GomokuState`、落子命令/事件、`GomokuRuleConfig` Odin 规则资产和 `NetworkGomokuMatch` Mirror 适配器。当前支持玩家落子、服务器裁决、棋盘同步、观战加入和观战权限隔离；正式回放文件与双实例运行验收仍待下一步。

---

## 〇、执行总则

1. **顺序不可打乱**：P0 → P1 → P2 严格串行；M2 已确认 `NetworkRoomManager` 全局场景切换不适合单端口多房间，因此从 P1 结果重映射为 `NetworkManager + RoomRegistry + NetworkMatch`。
2. **分支策略**：每个阶段独立分支，合回 `master` 后打 tag。
   ```text
   master ─ P0 ── P1 ── P2 ── P3/P4
             │feat/p0-fixes  │feat/p1-room-manager  │feat/p2-multiroom
   ```
3. **可验收才提交**：每阶段末跑该阶段验收清单（见各阶段末），并更新 `02_落地实施计划.md` 的勘误。
4. **遇到调研结论与实现不符时**：以实际编译/运行为准，把偏差记录到各阶段"偏差登记"栏，不要静默改设计。

---

## 一、任务依赖图

```text
P0.1 生成器幂等化 ─┬─ P0.2 Player 自愈 ── P0.3 config 字段处置
                   ├─ P0.4 ReturnToLobby 校验
                   ├─ P0.5 HUD 去重 ──────（需 P0.1 重建场景）
                   ├─ P0.6 git 基线
                   └─ P0.7 README
P1 组1 双 prefab 重建（依赖 P0.1）→ P1 组2 核心类 → P1 组3 调用方收敛 → P1 组4 联调
P2 M1 端口修复+列表 → P2 M2 多房间 → M3 生命周期 → M2-V 正式 UI
                                      └→ M3-S 同步基线 ✅
P4 测试与 CI（依赖当前架构定稿） → P5 回放基础（依赖事件模型）
P3 公网联机（按需）              → P6 物理同步（依赖真实 Rigidbody 玩法）
对局内核 ✅ → Gomoku 规则 ✅ → Mirror 对局桥接 → 回放文件/播放器
```

---

## 二、P0 — 地基修复（约 3 小时 / 半天）

> 设计依据：`02_落地实施计划.md` 第 P0 章。核心：修生成器幂等性缺陷（4 处 early-return 导致过期资产永不刷新）。

| # | 任务 | 改动文件 | 工时 | 依赖 |
|---|---|---|---|---|
| P0.1 | 新增 `Socket/Multiplayer/Regenerate Demo` 菜单（`AssetDatabase.DeleteAsset(Root)` + `Initialize()`，带确认框）；4 处 early-return 改"存在即删" | `Assets/Multiplayer/Editor/MultiplayerDemoInitializer.cs` | 30min | — |
| P0.2 | `NetworkPlayer.OnStartServer` 加 `if (characterController == null) characterController = GetComponent<CharacterController>();` | `Assets/Multiplayer/Runtime/Network/NetworkPlayer.cs:24-38` | 10min | P0.1（重建后验证） |
| P0.3 | 删 `NetworkPlayer.config` 字段，`FixedUpdate`/`SubmitInput` 改从 `(NetworkManager.singleton as SocketNetworkManager)?.Config` 取值（保守可跳过） | `NetworkPlayer.cs:15,28,53-64,130-137` | 20min | — |
| P0.4 | `ServerReturnToLobby(NetworkPlayer requester)` 加 `!_gameStarted || requester == null || !requester.IsLeader` 校验；`CmdRequestReturnToLobby` 传 `this`；`RoomOperations.ReturnToLobby` 统一走 command（堵死 host 直接调用路径） | `SocketNetworkManager.cs:179-187`、`NetworkPlayer.cs:111-116`、`RoomOperations.cs:26-32` | 30min | — |
| P0.5 | `CreateLobbyScene` 移除 `RoomStatusPanel`/`RoomChatPanel`/`CreateCanvas()`；`PcRoomHud.Awake` 加单实例防呆（可选） | `MultiplayerDemoInitializer.cs:161-175`、`PcRoomHud.cs:17-21` | 30min | P0.1 |
| P0.6 | 提交 `Assets/Multiplayer/Docs/`；删除或忽略 `MirrorExamplesPipelineConverted.txt` | git | 5min | — |
| P0.7 | README 修编号（列表 B 改 5-14）+ 加 **Quick Start（首次运行）** 节 + 修 smoke-test 的 `NetworkManagerHUD` 描述 + 标注"手工搭建"节 | `README_MULTIPLAYER.md:18-33,51` | 30min | — |

**P0 验收**：✅ 重跑 Initialize 后 `Player.prefab` 引用有效且角色用 CharacterController（碰撞生效）；✅ Host 只出现一份 PcRoomHud；✅ 非 leader 在游戏中的 ReturnToLobby 无效、leader 有效；✅ `Docs/` 已提交、残留 txt 已清理；✅ README 有 Quick Start 且编号正确。

---

## 三、P1 — 架构升级：继承 NetworkRoomManager（历史方案，已废止）

> 本节保留原始决策记录。实际运行架构以 M2 的 `SocketRoomManager : NetworkManager + RoomRegistry + MatchInterestManagement` 为准，不再执行本节的双 prefab 和全局场景切换方案。

> 设计依据：`02_落地实施计划.md` 第 P1 章。核心：`SocketRoomManager : NetworkRoomManager` + `SocketRoomPlayer : NetworkRoomPlayer` 双 prefab + `ReplacePlayerForConnection`，**绝不 override 那 8 个入口**。

### 第 1 组：地基（上午，约 2.5h）

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P1.1 | 重建生成器：生成 `RoomPlayer.prefab`（NetworkIdentity+SocketRoomPlayer）与 `Player.prefab`（NetworkIdentity+NetworkPlayer+LocalPlayerInput+NetworkTransformUnreliable+CharacterController）；修"已有即返回"分支与空引用 | `MultiplayerDemoInitializer.cs` | 1h |
| P1.2 | 重跑 `Initialize PC Demo`，`SocketRoomManager` 的 `RoomScene/GameplayScene` 接线正确（onlineScene==RoomScene） | 生成资产 | 0.5h |
| P1.3 | `MultiplayerConfig` 三场景字段加 `[Scene]`；`MultiplayerConfigEditor.DrawValidation` 加 Build Settings 校验提示 | `MultiplayerConfig.cs`、`MultiplayerConfigEditor.cs` | 0.5h |
| P1.4 | 确认 `lobbyScene/gameplayScene` 与 `RoomScene/GameplayScene` 一致 | 配置 | 0.5h |

### 第 2 组：核心类（第 1 天下午，约 4h）

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P1.5 | 新增 `SocketRoomPlayer.cs : NetworkRoomPlayer`：displayName/displayColor/isLeader/phase SyncVar + CmdSetDisplayName/CmdRequestStartGame/CmdRequestReturnToLobby/ServerSetLeader + override ReadyStateChanged/OnClientEnterRoom/OnClientExitRoom | 新文件 `Assets/Multiplayer/Runtime/Network/SocketRoomPlayer.cs` | 1.5h |
| P1.6 | 新增 `SocketRoomManager.cs : NetworkRoomManager`：Awake 填 RoomScene/GameplayScene；override `OnRoomStartServer/OnRoomServerConnect/OnRoomServerDisconnect/OnRoomServerCreateGamePlayer/OnRoomServerSceneLoadedForPlayer/OnRoomServerSceneChanged/OnRoomServerPlayersReady`；`EnsureRoomState/Chat` 生成后 `DontDestroyOnLoad`；`TryStartGame`/`ServerReturnToLobby`；`showRoomGUI=false` | 新文件 `Assets/Multiplayer/Runtime/Network/SocketRoomManager.cs` | 2h |
| P1.7 | 改造 `NetworkPlayer.cs`：删 ready/phase/CmdRequestStartGame/CmdRequestReturnToLobby；新增 `[Server] SetIdentity(name,color,isLeader)`；保留移动/交互/聊天 | `NetworkPlayer.cs` | 0.5h |

### 第 3 组：调用方收敛（第 2 天上午，约 3.5h）

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P1.8 | 删 `SocketNetworkManager.cs`；全局改 10 处类型引用（R1）；`NetworkRoomState.ServerRefresh` 签名（R6） | 10 个文件 | 1h |
| P1.9 | `RoomOperations.cs`：StartGame/ReturnToLobby/ToggleReady/SetReady 改走 `SocketRoomPlayer`（R4/R5/R7） | `RoomOperations.cs` | 0.5h |
| P1.10 | `PcRoomHud.cs`：Ready 按钮取 `SocketRoomPlayer`；CurrentPhase/ConnectedPlayerCount 保持（R2/R3） | `PcRoomHud.cs` | 0.5h |
| P1.11 | `RoomStatusPanel/LocalPlayerInput/SocketRoomDiscovery/NetworkInteractable/NetworkStartup/SessionOperations` 类型/API 对齐 | 6 个文件 | 1h |
| P1.12 | `MultiplayerDemoInitializer` 的 Bootstrap 场景补 `roomPlayerPrefab` 接线 | `MultiplayerDemoInitializer.cs` | 0.5h |

### 第 4 组：联调与回归（第 2 天下午，约 3h）

| # | 任务 | 工时 |
|---|---|---|
| P1.13 | Host+2 Client 冒烟：大厅加人/Ready/Start→切 Game→**身份（名字/颜色/Leader）保持**、移动正常 | 1h |
| P1.14 | Return to Lobby → 房间玩家换回、ready 复位、交互物重置 | 1h |
| P1.15 | 断线 Leader 转移、maxConnections 溢出断开、`allowLateJoiners` 两态、discovery 广播 phase/playerCount 正确 | 0.5h |

**P1 偏差登记**（遇到填这里）：________

**P1 验收**：见 `02_落地实施计划.md` §1.7 五条。

**明确不做（留 P2/P4）**：对局内晚加入直接加人（`OnRoomServerAddPlayer` 钩子）；兴趣管理多房间；单元测试。

---

## 四、P2 — 房间化：一端口多房间（约 41 小时 / 3 个工作日）

> 设计依据：`02_落地实施计划.md` 第 P2 章。核心：MatchInterestManagement + 单场景 + **Room 级 spawn 替代全局 ServerChangeScene**（最大陷阱）。

### M1：端口修复 + 发现接线 + 单房间列表（约 8.5h）— ✅ 已落地

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P2.1 | `RoomInfo` 加 `port`；`ProcessResponse` 填 `uri.Port` | `SocketRoomDiscovery.cs:33-43,75-93` | 1.5h |
| P2.2 | `SocketRoomManager.StartClient(RoomInfo)`（ApplyPort 在 StartClient 之前）+ `SessionOperations.JoinRoom` | `SocketRoomManager.cs`、`SessionOperations.cs` | 1.5h |
| P2.3 | 发现接线：服务器启动即自动广播（`Update` 轮询 `NetworkServer.active`）、列表激活→`StartDiscovery()`；`NetworkStartup` 兼容 | `SocketRoomDiscovery.cs`、`SocketRoomManager.cs` | 1h |
| P2.4 | TTL 心跳清理（`Dictionary<long, ServerEntry>` + `lastSeen > serverTtl`）+ `ServersChanged` 快照事件 | `SocketRoomDiscovery.cs` | 2h |
| P2.5 | PcRoomHud IMGUI 房间列表（Browse/Scan/Join） | `PcRoomHud.cs` | 2h |
| P2.6 | 手工验证：`-port 8888` host/join、下线移除、默认端口回归（**用户自测**） | — | 0.5h |

**M1 落地说明（2026-08-31）**：全部代码已按上述改动落盘并通过 `H:/unity2022.3.16f1` batchmode 编译（0 错误）；P2.6 手工验收由用户自测。生成的 Bootstrap 场景新增 `SocketRoomDiscovery`（DDOL manager 对象），出箱即有 LAN 浏览；README 已同步。**偏差登记见本页底部 P2 偏差栏。**

**M1 验收**：✅ 非默认端口主机可被发现且一键 Join 成功；✅ 关主机进程列表 TTL 内自动移除；✅ 刷新按钮+3s 自动刷新反映人数/房名变化；✅ 默认 7777 + localhost 直连回归可用。

### M2：MatchInterestManagement 多房间（约 22.5h）— ✅ 核心已落地

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P2.7 | 新 `RoomMessages.cs`（照抄 `MatchMessages.cs` 六操作 + MatchInfo/PlayerInfo） | 新文件 | 1.5h |
| P2.8 | 新 `SocketRoomManager.cs`：五张表（playerMatches/openMatches/matchConnections/playerInfos/waitingConnections）+ Create/Cancel/Join/Leave/Ready/Start + SendRoomList | 新文件 `Runtime/Network/SocketRoomManager.cs` | 6h |
| P2.9 | `SocketNetworkManager` 改造：autoCreatePlayer→0、OnServerAddPlayer 入大厅、phase 迁房间对象、Leader 按房过滤、删全局 EnsureRoom* | `SocketNetworkManager.cs` | 3h |
| P2.10 | `NetworkRoomState/Chat` 每房 spawn；`RoomStatusPanel/RoomChatPanel` 按本机玩家 matchId 取房（不再 FindFirstObjectByType） | `NetworkRoomState.cs`、`NetworkRoomChat.cs`、`RoomStatusPanel.cs`、`RoomChatPanel.cs` | 2.5h |
| P2.11 | `NetworkMatch` 打到全部预制体 + `LobbyMatchId` 大厅可见性 | 生成器+预制体 | 2h |
| P2.12 | `RoomEnvironment` 预制体 + `ServerStartRoom` Room 级 spawn（替代全局切场景主路） | 生成器+`SocketRoomManager.cs` | 3h |
| P2.13 | 发现 Response 改 `rooms[]` + 客户端合并/事件 | `SocketRoomDiscovery.cs` | 2.5h |
| P2.14 | uGUI `RoomListPanel` + `RoomInfoRow`（ScrollView+行预制体+Join 两段式） | 新 UI | 2h |

**M2 落地说明（2026-08-31）**：核心已按 `04_M2重新映射方案.md` 重调并落盘：`SocketRoomManager` 降回 `NetworkManager`，`RoomRegistry` 承担 Create/Join/Leave/Ready/Start/Return/Cancel 规则，Bootstrap 只保留一个物理 online scene，房间状态/聊天/交互物按 `NetworkMatch.matchId` 分房 spawn。生成器、配置、发现广播、HUD、状态面板、asmdef、Odin 配置入口均已同步；`RoomEnvironment` 可视化环境和 uGUI 房间列表仍留后续视觉化阶段。

**M2 验收**：✅ 单端口两客户端建两房，A 房看不到 B 房玩家/交互物（Match 隔离）；✅ 房间列表显示两房及人数/phase，入房正确隔离、聊天只在本房广播；✅ 房 A 房主 Start → A 房 phase=InGame+环境 spawn，房 B 保持大厅互不影响；✅ 连接后能收到 `ClientMatchMessage.List` 同步最新列表。

### M3：生命周期加固（进行中，约 10-12h；P2.16/P2.17 已完成）

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P2.15 | 空房回收 `RecycleEmptyRooms` + `roomIdleTimeout`；销毁房级状态、聊天和交互物 | `SocketRoomManager.cs`、`MultiplayerConfig.cs` | 2.5h |
| P2.16 | 心跳 TTL 加固 + `requestedRoom` 多房间过滤语义 | `SocketRoomDiscovery.cs` | 1.5h |
| P2.17 | 房主离开：按房 Leader 转移 / 最后一人回收 / `OnServerDisconnect` 全路由 | `SocketRoomManager.cs` | 2h |
| P2.18 | `MultiplayerConfig` 增加 `roomIdleTimeout` 并统一 `maxRooms/serverTtl` 校验 | `MultiplayerConfig.cs` | 1h |
| P2.19 | 断线/重连/中途加入回归；设计稳定玩家 ID 和恢复窗口 | `RoomRegistry.cs`、认证/会话层、测试 | 3h |

**M3 验收**：空房超时自动从列表消失且房级对象销毁；房主离开后房内成员接管或最后一人离开回收；服务器进程下线后客户端在 TTL 内移除；断线重连在恢复窗口内回到原房间，超时则进入大厅；中途加入不能看到其他房间对象。

> 🔍 **2026-09-11 核查修正**：✅ P2.16（TTL 心跳清理）与 P2.17（Leader 转移/最后一人回收/断线全路由）**已实现**（剩"只剩旁观者→无 Leader"边缘）；P2.15 原"空房超时回收"语义不存在（0 成员房即删），改为"**闲置房回收**"（Room 加 `LastActivityTime`）；P2.19 断线重连为 **0 实现**——断线即房籍丢失（玩家表以 connectionId 为键），稳定 ID=认证名字，恢复台账需以 stableId 为键、窗口期名字预占防抢名、房间删除条件补"无未过期恢复席位"；与 P2.15 合并设计。

### M3-S：同步基线（✅ 已落地，后续继续加固）

| # | 内容 | 当前状态 |
|---|---|---|
| S1 | 玩家移动服务器权威；`NetworkTransformUnreliable` 使用 Server→Client | ✅ |
| S2 | 输入帧带序号；服务端过滤重复、乱序和过快请求 | ✅ |
| S3 | 认证握手携带协议版本和配置签名 | ✅ |
| S4 | 网络消息字节数、消息数、实时速率和 RTT 统计 | ✅ |
| S5 | 房间操作、聊天、交互命令的服务端限频 | ⏳ |
| S6 | 错误码替代关键路径纯字符串；协议变更记录 | ⏳ |

**M3-S 验收**：两端配置不一致时认证失败并显示原因；客户端修改 Transform 不改变服务器状态；旧输入帧不会回写移动；异常高频 Command 不会拖垮服务器；指标可在调试 HUD 中观察。

### M4：通用对局内核与五子棋首版（部分完成，约 1-2 天）

| # | 任务 | 改动文件 | 状态 |
|---|---|---|---|
| M4.1 | 通用 `MatchSession`：参与者、权限、阶段、命令裁决、确认事件序列 | `Runtime/Match/` | ✅ |
| M4.2 | 通用状态快照、事件存储和离线回放游标 | `MatchSession.cs`、`MatchReplay.cs` | ✅ |
| M4.3 | 五子棋规则：棋盘、轮流落子、越界/占用/胜负/和棋校验 | `Runtime/Games/Gomoku/` | ✅ |
| M4.4 | 五子棋 Odin 规则资产和可点击棋盘预览 | `GomokuRuleConfig.cs` | ✅ |
| M4.5 | Mirror 对局桥接：房间绑定 `MatchSession`，玩家落子 Command 和观战快照 | `NetworkGomokuMatch.cs`、`SocketRoomManager.cs` | ✅ |
| M4.6 | 五子棋完整联机验收：玩家、观战、断线、重连、事件同步 | Mirror 桥接、PlayMode 测试 | ⏳ |

**M4 边界**：`MatchSession` 和 `GomokuRules` 不依赖 Mirror/Scene/GameObject；Mirror 只负责消息、权限入口和快照发送；Odin 只负责规则配置、编辑器预览和调试，不持有服务器状态。

> 🔍 **2026-09-11 核查修正（M4.6 前置缺口）**：主链路可跑，但验收前需修：① 轮到断线方时对局**死锁**（无跳过/认输机制，`GomokuRules` 只认当前座位）；② `MatchSession.SetParticipantConnection` 为**死分支**（断线不更新参与者状态）；③ `RpcStoneConfirmed` 不带事件序号、线上无事件日志（P5 回放无数据源）；④ `ServerStartMatch` 失败无回滚（房间停在 InGame）。另：观战者不落位（`JoinRoomAsSpectator` 未调 `PlacePlayerInRoom`）。

**P2 偏差登记**：M1（2026-08-31）——① `SocketNetworkManager` 已随 P1 删除，P2.2/P2.3 落在 `SocketRoomManager` 上；② 自动广播不 override `OnStartServer`（P1 红线：不改 8 个入口方法），改在 `SocketRoomDiscovery.Update()` 轮询 `NetworkServer.active` 的 false→true 沿触发 `AdvertiseServer()`、true→false 沿关闭 socket；headless 服务器仍由基类 `Start()` 自动广播；③ 端口回归不放在 `SocketRoomManager.StartClient(string)`（会覆盖 `NetworkStartup` 的 `-port 8888 -client` 顺序），改放 `SessionOperations.StartClient()`（HUD 直连路径）；④ `serverTtl` 从 M3 P2.18 提前到 M1 使用（TTL 裁剪所需）；⑤ 保留 `OnRoomFound` 兼容事件，新增 `Servers` 快照 + `ServersChanged`；⑥ `StopBrowsing` 一并清空缓存表，`ServersChanged` 在增/删/变时触发。

---

## 五、M2-V：可视化与正式 UI（待落地，约 1-2 天）

> M2 的网络核心已经可用，但当前仍以共享大厅场景和 `PcRoomHud` IMGUI 为主。本阶段解决“能联机”到“可交付使用”的差距。

| # | 任务 | 改动文件 | 状态 |
|---|---|---|---|
| V1 | 将房间地面、装饰、出生点等静态内容整理为 `RoomEnvironment` 预制体 | 生成器、Prefab、`SocketRoomManager.cs` | ⏳ |
| V2 | 正式 uGUI 房间列表：滚动列表、房间人数、阶段、加入按钮 | 新增 `RoomListPanel`、`RoomInfoRow` | ⏳ |
| V3 | 完整连接状态：连接中、认证失败、房间已满、房间不存在、断线返回 | UI、会话层 | ⏳ |
| V4 | 配置中心增加同步诊断页：协议版本、配置签名、RTT、上下行速率 | Odin 编辑器窗口 | ⏳ |

**M2-V 验收**：不依赖 IMGUI 也能完成发现、创建、加入、准备、开始、返回和断线提示；房间环境只对所属房间可见；窄窗口下无控件遮挡。

> 🔍 **2026-09-11 核查修正**：V1/V2 为 0 实现（仅 IMGUI `PcRoomHud`）；V3 仅房间拒绝路径有提示（认证失败/连接中/断线无 UI）；V4 Odin 窗口已有 4 页，仅缺"同步诊断"页。**前置**：Bootstrap 场景过期（缺 `SocketRoomDiscovery`）→ 先跑 Regenerate。生成器会覆盖配置/重建场景，UI 必须走"预制体 + 生成器接线"。

---

## 六、P3 — 商业化联机（按需，约 1-2 天）

> 设计依据：`02_落地实施计划.md` 第 P3 章。推荐路径 A（Edgegap 全托管，零新包）。

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P3.1 | 注册/配置 Edgegap 账户，编辑器窗口填 API token、配 Dedicated Server 容器 | `Assets/Mirror/Hosting/Edgegap/Editor/EdgegapWindowV2.cs` | 0.5h |
| P3.2 | Bootstrap 换 `EdgegapLobbyKcpTransport`，填 `lobbyUrl`；保留 `SocketRoomManager`/`SocketAuthenticator`/`NetworkStartup` | Bootstrap 场景 | 1h |
| P3.3 | `MultiplayerConfig` 加 `usePublicMatchmaking`；true 时隐藏/禁用 `SocketRoomDiscovery` | `MultiplayerConfig.cs` | 0.5h |
| P3.4 | `ApplyPort` 适配 relay（relay 模式跳过端口应用/改用 relay 端口） | `SocketRoomManager.cs` | 0.5h |
| P3.5 | 会话入口：复用/改造 `UILobbyCreate/UILobbyList/UILobbyEntry/UILobbyStatus`；创建 `SetServerLobbyParams+StartHost`、加入 `networkAddress=lobby_id+StartClient` | UI | 2-3h |
| P3.6 | 在 `SocketAuthenticator` 上增加密码/Token 校验与认证超时；保留 `authenticationData` 玩家身份 | `SocketAuthenticator.cs`、`NetworkStartup.cs` | 2h |
| P3.7 | 双实例 relay 冒烟：host 走 Lobby 建房 → 客户端输码加入 → RoomState/Interactable/Ready/开始/返回全链路 | — | 1h |
| P3.8 | 更新 README "intentionally separate services" 表述为已落地 | `README_MULTIPLAYER.md:14` | 0.25h |

**P3 验收**：✅ relay 建房+输码加入全链路通；✅ 密码房生效、`-password` 贯通；✅ 公网模式下 LAN 发现被禁用；✅ README 更新。

**P3 后续（非必须）**：`EncryptionTransport` 加密认证消息；Unity Relay / Steam 作为 B 计划评估备忘。

---

## 七、P4 — 工程化：测试与 CI（约 1-2 天）

> 设计依据：`02_落地实施计划.md` 第 P4 章。核心：先补 asmdef，再照抄 MemoryTransport，EditMode 直测 `[Server]` 方法。

| # | 任务 | 改动文件 | 工时 |
|---|---|---|---|
| P4.1 | Runtime/Editor/Tests asmdef 已创建；确认依赖边界和生成 csproj 正确 | `Assets/Multiplayer/*/*.asmdef` | ✅ |
| P4.2 | 引入 `MemoryTransport` 测试基座和精简 Mirror 测试辅助类 | `Assets/Multiplayer/Tests/Common/` | ⏳ |
| P4.3 | 扩展 `RoomRegistry`、`InputAdmission`、协议签名、认证和租约边界测试 | `Assets/Multiplayer/Tests/Editor/` | 部分完成 |
| P4.4 | `InteractionLeaseRules` 纯逻辑 EditMode 测试：先到先得/重入/超时/越界/占用中/CanRelease（5-8 用例） | `Tests/EditMode/InteractionLeaseRulesTests.cs` | 1h |
| P4.5 | `NetworkInteractable` 小重构：`ServerAdvanceLease(float now)` 提取（**唯一生产代码改动**）→ ServerTryAcquire/ServerRelease/ServerReleaseById/ServerReset/租约过期 EditMode 测试 | `NetworkInteractable.cs`、`Tests/EditMode/NetworkInteractableTests.cs` | 2h |
| P4.6 | 认证串联测试：`TimeoutAuthenticator` + 内层认证器启动/超时断连 | `Tests/` | 0.5h |
| P4.7 | 冒烟两级：EditMode 状态机集成测试（不切场景，断言 Phase 全流转+RoomState+Leader+all-ready 门禁）+ 手动 `Smoke Test Room Flow` 菜单 | `Tests/`、Editor 菜单 | 1.5h |
| P4.8 | CI：`ci/PureLogic.Tests/`（dotnet test 秒级）+ `run-tests.ps1`（EditMode 全量+可选 PlayMode）+ pre-commit（只跑 dotnet test）+ `.gitignore` 补 `TestResults/` | `ci/`、`.git/hooks/pre-commit`、`.gitignore` | 1.5h |

**P4 验收**：`dotnet test` 纯逻辑全绿（秒级）；Unity EditMode 全量绿；pre-commit 只跑纯逻辑不拖慢提交；CI 解析 `-testResults` XML（`result="Failed"`）而非只看进程退出码。当前仅完成测试程序集编译和部分 EditMode 用例，尚未完成 Test Runner/CI 验收。

> 🔍 **2026-09-11 核查修正**：① `Socket.Multiplayer.Tests.asmdef` 缺 Mirror 家族引用（阻塞 P4.5-P4.7 集成测试）；② `MemoryTransport` 源丢失（Temp 备份被删且未入库）→ 上游取 v96 版本，命名空间改 `Socket.Multiplayer.Tests.Support`；③ `InteractionLeaseRules` 已纯化可直接测，`NetworkInteractable` 需提 `ServerAdvanceLease(float now)`；④ 冒烟断言用 `Lobby→InGame→Lobby`（`RoomPhase.Starting/ReturningToLobby` 永不产生）；⑤ `.gitignore` 需 `!ci/**/*.csproj`，CI 必须解析 testResults XML。

---

## 八、P5 — 回放基础（按需，约 2-3 天）

> 只有出现观战、审计、教学复盘或问题追踪需求时启动。回放不连接 Mirror，避免把实时网络组件变成播放器。

| # | 任务 | 设计边界 | 状态 |
|---|---|---|---|
| P5.1 | 服务器确认事件流 | 记录创建、加入、离开、准备、开始、交互、生成、销毁等已确认事件 | ⏳ |
| P5.2 | 周期关键帧 | 每 1-2 秒记录玩家/实体位置、旋转、速度和核心状态 | ⏳ |
| P5.3 | 回放文件格式 | 文件头包含协议版本、构建版本、模板签名、随机种子；支持分段和大小上限 | ⏳ |
| P5.4 | 离线播放器 | 根据事件和关键帧驱动本地表现，不启动网络、不执行客户端 Command | ⏳ |
| P5.5 | 回放兼容策略 | 版本不兼容时拒绝播放，保留格式版本和迁移入口 | ⏳ |

**P5 验收**：录制过程不显著影响实时帧率；异常结束的文件可识别为未完成；回放能定位到任意关键帧；同一文件在没有服务器的机器上可播放。

---

## 九、P6 — 物理同步与预测（按玩法触发，约 1-3 天）

> 当前玩家使用 `CharacterController`，不在玩家 Prefab 上添加 Rigidbody。只有出现真实刚体玩法时才启动本阶段。

| # | 任务 | 设计边界 | 状态 |
|---|---|---|---|
| P6.1 | 物理对象分类 | 门/阀门同步状态参数；箱子/球/车辆才同步 Rigidbody | ⏳ |
| P6.2 | 服务器物理权威 | 服务器运行 Rigidbody，客户端接收 `NetworkRigidbodyUnreliable` 状态 | ⏳ |
| P6.3 | 高响应对象预测 | 仅对需要本地立即反馈的对象评估 `PredictedRigidbody` | ⏳ |
| P6.4 | 物理回归测试 | 碰撞、推挤、断线、房间隔离、服务器纠正 | ⏳ |

**P6 验收**：服务器最终状态唯一可信；客户端预测误差可纠正；物理对象不会跨房间可见；没有为玩家增加第二套运动权威。

---

## 十、进度追踪表（复制到每个阶段开始前）

| 阶段 | 开始日 | 计划完成 | 实际完成 | 提交 | 偏差记录 |
|---|---|---|---|---|---|
| P0 | __ | __ | __ | __ | __ |
| P1 | __ | __ | __ | __ | __ |
| P2 | __ | __ | __ | __ | __ |
| M3-S | 2026-08-31 | 2026-08-31 | ✅ | — | 同步基线已落地，限频/错误码待补 |
| M2-V | __ | __ | __ | __ | __ |
| P3 | __ | __ | __ | __ | __ |
| P4 | __ | __ | 部分完成 | __ | 测试程序集可编译，Unity Test Runner/CI 待做 |
| P5 | 按需 | __ | __ | __ | 依赖事件模型 |
| P6 | 按需 | __ | __ | __ | 依赖真实 Rigidbody 玩法 |

## 十一、关键文件速查

| 文件 | 作用 |
|---|---|
| `Assets/Multiplayer/Editor/MultiplayerDemoInitializer.cs` | 生成器；P0.1 幂等化、P1.1 双 prefab、P2.11/2.12 预制体 |
| `Assets/Multiplayer/Runtime/Network/SocketRoomManager.cs` | 网络生命周期、房间消息、房级对象生成与清理 |
| `Assets/Multiplayer/Runtime/Network/NetworkPlayer.cs` | 服务器权威移动、输入帧与房间身份 |
| `Assets/Multiplayer/Runtime/Network/SocketRoomDiscovery.cs` | 端口、心跳、rooms[] 和 LAN 发现 |
| `Assets/Multiplayer/Runtime/Network/NetworkInteractable.cs` | 房间交互、租约和后续物理交互入口 |
| `Assets/Multiplayer/Runtime/Network/InputAdmission.cs` | 输入序号、服务端限频和乱序过滤 |
| `Assets/Multiplayer/Runtime/Network/MultiplayerProtocol.cs` | 协议版本和配置签名 |
| `Assets/Multiplayer/Runtime/Network/SocketAuthenticator.cs` | Demo 认证、协议兼容和玩家名称占用 |
| `Assets/Multiplayer/Runtime/Network/SocketNetworkMetrics.cs` | RTT、消息和带宽诊断 |
| `Assets/Multiplayer/Runtime/Gameplay/InteractionLeaseRules.cs` | P4.4 纯逻辑测试 + CI dotnet test 直引源文件 |
| `Assets/Mirror/Components/NetworkRoomManager.cs` | 旧方案调研参考，当前运行时不再继承 |
| `Assets/Mirror/Examples/MultipleMatches/Scripts/MatchMessages.cs` | P2.7 六操作协议照抄对象 |
| `Assets/Mirror/Transports/Edgegap/EdgegapLobby/EdgegapLobbyKcpTransport.cs` | P3.2 核心传输 |
| `Temp/MirrorTestsBackup-20260830/Common/MemoryTransport.cs` | P4.2 测试基座照抄源（⚠️ 本地备份已丢失，2026-09-11 核查；改从 Mirror 上游 v96 取源） |
